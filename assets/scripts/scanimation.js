window.addEventListener('scroll', function() {
    let animacion = document.getElementById("mambanimado");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entrada 1s ease-in-out forwards '
    }

})
window.addEventListener('scroll', function() {
    let animacion = document.getElementById("eduanimado");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entrada 1s ease-in-out forwards'
    }

})
window.addEventListener('scroll', function() {
    let animacion = document.getElementById("saludanimado");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entrada 1s ease-in-out forwards'
    }

})
window.addEventListener('scroll', function() {
    let animacion = document.getElementById("justiciaanimado");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entrada 1s ease-in-out forwards'
    }

})
window.addEventListener('scroll', function() {
    let animacion = document.getElementById("textmambanimado");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entradaizquierda 1s ease-in-out forwards'
    }

})

window.addEventListener('scroll', function() {
    let animacion = document.getElementById("texteduanimado");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entradaderecha 1s ease-in-out forwards'
    }

})
window.addEventListener('scroll', function() {
    let animacion = document.getElementById("textsaludanimado");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entradaizquierda 1s ease-in-out forwards'
    }

})

window.addEventListener('scroll', function() {
    let animacion = document.getElementById("textjusticianimado");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entradaderecha 1s ease-in-out forwards'
    }

})
window.addEventListener('scroll', function() {
    let animacion = document.getElementById("textoagradecimiento");
    let posicionimg = animacion.getBoundingClientRect().top;
    console.log(posicionimg);
    let tamañoDePantalla = window.innerHeight / 1.5;
    if (posicionimg < tamañoDePantalla) {
        animacion.style.animation = 'entradaizquierda 1s ease-in-out forwards'
    }

})